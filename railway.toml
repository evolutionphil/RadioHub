[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --max-old-space-size=8192 --expose-gc dist/index.js"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 5
