[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --max-old-space-size=4096 --max-semi-space-size=64 --expose-gc --perf-basic-prof dist/index.js"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 5
